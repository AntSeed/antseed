{
  "name": "@antseed/provider-veo",
  "version": "0.1.0-beta.0",
  "description": "Google Veo video generation provider for AntSeed",
  "type": "module",
  "files": ["dist"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "node ../../scripts/remove-paths.mjs dist",
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@antseed/node": ">=0.2.114"
  },
  "devDependencies": {
    "@antseed/node": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
